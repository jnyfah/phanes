module;

#include <iostream>

module builder;

void output() {
    std::cout << "Modules\n";
}

DirectoryTree build_tree(const std::filesystem::path& root) {

    // capture start time and store 

    // convert root to absolute path 

    // creat empty DT 

    // validate root if invalid return empty DT and record error 


}

